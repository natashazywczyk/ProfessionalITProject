const HomePage = () => {
    return (
        <div>
        {/* Insert Home Page content, providing intro to app and allow users to log in*/}
            <h1>Welcome</h1>
        </div>
    );
}  

export default HomePage;