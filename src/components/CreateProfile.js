import React from 'react';

const CreateProfile = () => {
    return (
        {/*Page where user can make a player profile to store stats from quiz, stored in database */}
    );
}

export default CreateProfile;